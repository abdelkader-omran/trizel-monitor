MODE: MANUAL (GitHub Web UI)
TOOL: GitHub Repository Editor (NOT Copilot)

WHERE:
Repository: trizel-monitor
Path: .github/workflows/daily.yml
Branch: main

HOW:
1. Exit Copilot Chat completely.
2. In GitHub Web UI:
   Code → .github/workflows → daily.yml → Edit.
3. Replace the FULL file content with the approved YAML (only YAML, no comments or headers).
4. Click "Commit changes".
5. Select "Create a new branch".
6. Branch name: fix/exit-code-contract
7. Commit.

NEXT:
8. Open Pull Request: fix/exit-code-contract → main
9. Merge PR into main.
10. Actions → Daily Official Data Monitor → Run workflow (branch: main).

RULES:
- Do NOT edit via Copilot.
- Do NOT include TARGET/OBJECTIVE/CONSTRAINTS text in the file.
- The file must contain YAML only.